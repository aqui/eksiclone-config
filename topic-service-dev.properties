# Server Settings
server.port=8766

# Application Settings
spring.application.name=topic-service
spring.datasource.url=${DB_URL:jdbc:mariadb://localhost:3306/eksidb?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:eksiclone_user}
spring.datasource.password=${DB_PASSWORD:eksiclone_password}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA Settings
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
spring.jpa.show-sql=true

# Flyway Settings (optional)
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Bean Management
spring.main.allow-bean-definition-overriding=true

# Logging Settings
logging.level.org.springframework.security=INFO
logging.level.org.springframework.cloud.gateway=INFO

# Swagger Settings
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.operations-sorter=alpha
springdoc.api-docs.path=/v3/api-docs

# Actuator Settings
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# RabbitMQ Settings (optional if already in bootstrap)
rabbitmq.exchange.name=eksiclone-exchange
rabbitmq.queue.topic-events=topic-events-queue
rabbitmq.routing.topic-create=topic.create
rabbitmq.routing.topic-update=topic.update
rabbitmq.routing.topic-delete=topic.delete
